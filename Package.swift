import PackageDescription

let package = Package(
    name: "TDTabView",
    products: [
        .library(
            name: "TDTabView",
            targets: ["TDTabView"]),
    ],
    targets: [
        .target(
            name: "TDTabView",
            dependencies: []),
    ]
)
